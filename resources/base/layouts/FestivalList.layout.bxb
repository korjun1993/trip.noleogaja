layout-macro-def (festival-result) {
  params {
    param (festivalList) {
      type (FestivalList)
      min (Required) max (One)
    }
  }
  content {
    image-card {
      aspect-ratio (16:9)
      image-url("[#{value(festivalList.festivals.firstImage)}]")
      title-area {
        halign (Start)
        slot1 {
          text {
            value("[#{value(festivalList.festivals.title)}]")
            style (Title_L)
          }
        }
      }
    }
  }
}