structure (FestivalList) {
  description (페이지넘버, 카운트 추가)
  property (pageNo) {
    min (Required)
    type (PageNo)
  }
  property (totalCount) {
    min (Required)
    type (TotalCount)
  }

  property (festivals) {
    min (Optional)
    max (Many)
    type (Festivals)
  }
}
