structure (FestivalList) {
  description (백준이형 request)
  property (contentId) {
    min (Required)
    type (ContentId)
  }

  property (title) {
    min (Required)
    type (Title)
  }

  property (beginDate) {
    min (Required)
    type (BeginDate)
  }

  property (endDate) {
    min (Required)
    type (EndDate)
  }

  property (image) {
    min (Required)
    type (Image)
  }

  property (location) {
    min (Required)
    type (Location)
  }
}
