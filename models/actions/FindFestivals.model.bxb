action (FindFestivals) {
  type(Search)
  collect {
    input (location) {
      type (location)
    }

    input (dateTimeExpression) {
          type (time.DateTimeExpression)
          min (Optional) max (Many)
    }

  }
  output (FestivalList)
}
