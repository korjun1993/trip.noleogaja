action (FindFestivals) {
  type(Search)
  collect {
    input (location) {
      type (Location)
    }

    input (dateTimeExpression) {
          type (time.DateTimeExpression)
          min (Optional) max (One)
    }

  }
  output (Festivals)
}
