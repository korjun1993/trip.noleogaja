action (ChangeLayout) {
  type(Search)
  description ("상세페이지에서 목록으로 이동")
  collect {
    input (layoutType) {
      type (LayoutType)
      min (Required) max (One)
    }
    input (festivalList) {
      type (FestivalList)
      min (Required) max (One)
    }
  }
  output (FestivalList)
}
